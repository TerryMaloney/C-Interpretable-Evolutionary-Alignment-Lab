pandas>=2.0.0
geopandas>=0.14.0
requests>=2.31.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
shapely>=2.0.0
pyproj>=3.6.0
rasterio>=1.3.0
fiona>=1.9.0
matplotlib>=3.7.0
folium>=0.14.0
contextily>=1.4.0
pyarrow>=13.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
openpyxl>=3.1.0
xlrd>=2.0.1
geopy>=2.4.0
branca>=0.7.0
# Layer 18 — NASA FIRMS
# (no extra deps — uses requests + pandas CSV)
# Layer 19 — NOAA DART buoys
# (no extra deps — uses requests + pandas)
# Layer 20 — GPS-TEC IONEX
georinex>=1.16.0
xarray>=2023.1.0
# Layer 21 — ESA Sentinel-5P
sentinelsat>=1.3.0
netCDF4>=1.6.0
# Layer 22 — CTBTO (station coordinates only; data restricted)
# (no extra deps)
# Layer 23 — NASA GRACE gravity
earthaccess>=0.8.0
# Layer 24 — VLF/ELF stubs (no deps; data licensing required)
# Layer 25 — NOAA GOES IR gravity waves
goes2go>=2023.8.0
# Masking layers
boto3>=1.34.0          # AWS S3 for FIRMS + GOES data
# Layers 26-35 (Sprint 7)
# Layer 26 — Movebank (no extra deps beyond requests/pandas)
# Layer 27 — BFRO (no extra deps)
# Layer 28 — USGS Mines (pandas)
# Layer 29 — FAA Wildlife Strikes (pandas)
# Layer 30 — USGS Water Wells (requests)
dataretrieval>=0.9.0   # USGS NWIS groundwater data (optional, for live well levels)
# Layer 31 — ADS-B (requests)
# Layer 32 — Space Weather (requests)
# Layer 33 — FOIA Docs (requests)
# Layer 34 — Maritime (requests)
# Layer 35 — Schumann (requests)
